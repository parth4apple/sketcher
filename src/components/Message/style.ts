import styled from "styled-components";

export const Text = styled.div`
    font-size: 20px;
    position: absolute;
    color: white;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    pointer-events: none;
`;